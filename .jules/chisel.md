## 2024-05-30 - Extract Pure Parsing Functions **Learning:** Refactoring out mapping logic without tracking edge cases (like implicitly returning empty values when conditionals change) can introduce subtle regressions in data ingestion loops. **Action:** Always meticulously track implicit validations inside original structural blocks (like `if key != "" && val != ""` safety checks) and enforce them in the extracted pure function return paths.
